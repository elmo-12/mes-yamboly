import { TiempoRealPage } from '@/features/realtime/components/TiempoRealPage';

/** `(app)/tiempo-real` — hub operativo (Figma 2156:3936, RF1/RF6). */
export default function Page() {
  return <TiempoRealPage />;
}
