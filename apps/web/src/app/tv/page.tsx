import { ModoTvPage } from '@/features/realtime/components/ModoTvPage';

/** `/tv` — tablero de planta (Figma 2163:8523), fuera del shell y solo lectura. */
export default function Page() {
  return <ModoTvPage />;
}
